# Indian Plate OCR Requirements 

# Python >= 3.10


# Core (inference only for deployment)
fast-plate-ocr==1.1.0
opencv-python-headless==4.13.0.92
numpy==2.0.2


# Training (only needed to retrain) 
# fast-plate-ocr[train]==1.1.0
# torch                          # PyTorch backend (recommended)
# torchvision
# keras>=3.10.0
# tensorflow==2.21.0             # for ONNX export only
# tf2onnx==1.17.0
# albumentations==2.0.8
# pandas==2.3.3
# scikit-learn==1.7.2


# Dataset generation (only needed to regenerate plates)
# Pillow==12.0.0                 # already pulled in by other deps


# ONNX export & patching
# onnx==1.17.0                   # already installed with fast-plate-ocr[train]
# onnxruntime                    # CPU inference
# onnxslim>=0.1.56

# Evaluation script
pandas==2.3.3

